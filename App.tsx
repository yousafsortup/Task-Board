/**
 * Root entry re-export.
 *
 * The real application lives in `src/app/App.tsx`; this file exists only
 * because the React Native template convention points `index.js` here.
 */
export { App as default } from './src/app/App';
